# Core
pdfplumber==0.11.9      # digital PDF text extraction
pytesseract==0.3.13     # OCR (needs the Tesseract binary installed separately)
pdf2image==1.17.0       # rasterise PDF pages for OCR (needs Poppler installed separately)
Pillow>=10.0.0          # image handling
reportlab==4.4.10       # generating the synthetic test PDFs

# API (optional — only needed to run api.py)
Flask==3.1.3
flask-cors==6.0.2
